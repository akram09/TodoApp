buildscript {
    apply from: 'versions.gradle'
    addRepos(repositories)
    dependencies {
        classpath deps.plugins.kotlin_gradle_plugin
        classpath deps.plugins.serialization
        classpath deps.plugins.android_gradle_plugin
    }
}

allprojects {
  addRepos(repositories)
}
